meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/Auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "Admin123!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
